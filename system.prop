# system.prop for jflte
#

# cannot take spaces
rild.libargs=-d /dev/smd0

# disable unsupported services
config.disable_consumerir=true

# NFC hall
ro.hardware.nfc_nci=bcm2079x

# disable sdcardfs usage (does not work with the current kernel)
ro.sys.sdcardfs=false
